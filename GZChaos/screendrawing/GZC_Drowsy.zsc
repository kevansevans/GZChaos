class GZC_Drowsy : EventHandler
{
	bool enabled;

	double tiredness;
	
	override void RenderOverlay(RenderEvent event)
	{
		if (!enabled) return;
		
		Screen.dim(Color("000000"), tiredness / 100, 0, 0, Screen.getWidth(), Screen.getHeight());
	}
}